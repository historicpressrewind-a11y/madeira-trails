{
  "name": "madeira-trails",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "refresh": "node scripts/refresh.js",
    "enrich": "node scripts/enrich.js",
    "build:index": "node scripts/build_index.js",
    "export:sqlite": "node scripts/export_sqlite.js",
    "daily": "npm run refresh && npm run enrich && npm run build:index && npm run export:sqlite"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "axios": "^1.7.7",
    "cheerio": "^1.0.0-rc.12",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pdf-parse": "^1.1.1",
    "better-sqlite3": "^9.5.0"
  }
}
